# Server
API_PORT=8080

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=vframotp
DB_PASS=secret
DB_NAME=vframotp

# Redis
REDIS_ADDR=localhost:6379

# JWT
JWT_SECRET=change_me_in_production_use_random_32_chars

# Worker
WORKER_CONCURRENCY=10

# Environment: development | production
APP_ENV=development
